import React from 'react';

const AddForm = (props) => {

    const task = null;
    return (
        <div>
            <input />
            <button>➕ Add task</button>
        </div>
    )
}

export default AddForm;